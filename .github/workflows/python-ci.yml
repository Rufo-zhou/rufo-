name: Python CI
"on": [push, pull_request, workflow_dispatch]
jobs: {check: {runs-on: ubuntu-latest, steps: [{uses: actions/checkout@v4}, {run: "python3 -m py_compile 'multi_agent_poster_system (1).py'"}]}}
